# API & Agent
anthropic>=0.30.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
sse-starlette>=2.1.0

# PDF Processing
pdfplumber>=0.11.0
pymupdf>=1.24.0          # fitz — faster image/screenshot extraction
Pillow>=10.3.0

# Vector Store
qdrant-client>=1.9.0

# Embeddings (local, no external API needed)
sentence-transformers>=3.0.0
torch>=2.2.0             # CPU-only is fine for inference

# SQL Store
aiosqlite>=0.20.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.7.0
httpx>=0.27.0
tenacity>=8.3.0
rich>=13.7.0             # pretty CLI output during ingestion
tqdm>=4.66.0
